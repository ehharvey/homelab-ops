services:
  web:
    build: .
    ports:
      - "8080:8080"
    environment:
      - PORT=8080
      - CONFIG_REPO_URL=git://config-repo/fleet.git
      - CONFIG_REPO_REF=main
    depends_on:
      - config-repo

  config-repo:
    build: ./dev/git-fixture
