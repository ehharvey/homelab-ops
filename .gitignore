venv/
__pycache__/
*.pyc
.terragrunt-cache/
.terraform/
*.tfstate
*.tfstate.*
.devcontainer/host-setup/.trust-token

# Go build artifacts
bin/

# Bootstrap CLI generated artifacts (certs, seeds, images) — never commit private keys
bootstrap-output/

# Locally downloaded base IncusOS images, used with scripts/validate-issue-5.sh
*.img

# Local LSP MCP port file, which contains the port number of the MCP server started by the bootstrap CLI. This file is used by the test suite to connect to the MCP server, and should not be committed to version control.
.lsp_mcp.port